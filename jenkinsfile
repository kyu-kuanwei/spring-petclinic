pipeline {
    agent any
    stages {
        stage("Build") {
            agent any
            steps {
                sh './mvnw clean package'
            }
            post {
                success {
                    archiveArtifacts 'target/*.jar'
                }
            }
        }

        stage("Copy Artifact") {
            steps {
                // script {
                //     step ([$class: 'CopyArtifact',
                //     projectName: env.JOB_NAME,
                //     filter: "archive/target/*.jar",
                //     target: 'artifact']);
                // }
                echo(env.projectName);
            }
        }

        // stage('Deploy Ansible') {
        //     steps {
        //         ansiblePlaybook playbook: 'main.yaml'
        //     }
        // }
    }
}